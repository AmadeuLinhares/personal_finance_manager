meta {
  name: Delete scheduled item
  type: http
  seq: 10
}

delete {
  url: {{baseUrl}}/scheduled-items/{{createdScheduledItemId}}
  body: none
  auth: inherit
}

docs {
  Keeps the transactions the rule already produced and just unlinks them.
}
