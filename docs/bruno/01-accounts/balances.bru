meta {
  name: All balances + totals
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/accounts/balances?asOf={{today}}
  body: none
  auth: inherit
}

params:query {
  asOf: {{today}}
  ~includeArchived: false
}

assert {
  res.status: eq 200
}

docs {
  The dashboard header in one request.
}
