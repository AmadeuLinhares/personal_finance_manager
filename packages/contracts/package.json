{
  "name": "@pfm/contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The API's data contract: one source of truth for the Express API and the React client.",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "lint": "eslint",
    "lint:fix": "eslint --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@pfm/eslint-config": "workspace:*",
    "eslint": "^10.8.0",
    "prettier": "^3.9.6",
    "typescript": "~6.0.2"
  }
}
